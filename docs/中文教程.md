# ZeroClaw 学习教程

> 本教程带你从零开始学习 ZeroClaw —— 一个高性能、低资源占用的 Rust 原生 AI 代理运行时框架。

---

## 目录

1. [项目概述](#1-项目概述)
2. [快速开始](#2-快速开始)
3. [核心架构](#3-核心架构)
4. [配置系统](#4-配置系统)
5. [Provider 系统](#5-provider-系统)
6. [Channel 系统](#6-channel-系统)
7. [Tool 系统](#7-tool-系统)
8. [Memory 系统](#8-memory-系统)
9. [Agent 运行时](#9-agent-运行时)
10. [安全机制](#10-安全机制)
11. [硬件外设](#11-硬件外设)
12. [技能系统](#12-技能系统)
13. [实战练习](#13-实战练习)
14. [进阶主题](#14-进阶主题)

---

## 1. 项目概述

### 1.1 什么是 ZeroClaw？

ZeroClaw 是一个**面向智能代理工作流的运行时操作系统**。它抽象了模型、工具、记忆和执行层，使 AI 代理可以"一次构建、随处运行"。

### 1.2 核心特点

| 特点 | 说明 |
|------|------|
| **Rust 原生** | 单二进制部署，无运行时依赖 |
| **轻量级** | 内存占用 < 5MB，二进制约 8.8MB |
| **快速启动** | 冷启动 < 10ms（0.8GHz 边缘 CPU） |
| **跨架构** | 支持 ARM / x86 / RISC-V |
| **可替换架构** | Provider/Channel/Tool/Memory 全部可插拔 |
| **安全优先** | 配对鉴权、沙箱、allowlist、速率限制 |

### 1.3 与其他框架对比

| | OpenClaw | NanoBot | PicoClaw | ZeroClaw 🦀 |
|---|---|---|---|---|
| **语言** | TypeScript | Python | Go | **Rust** |
| **RAM** | > 1GB | > 100MB | < 10MB | **< 5MB** |
| **启动时间** | > 500s | > 30s | < 1s | **< 10ms** |
| **二进制体积** | ~28MB | N/A | ~8MB | **~8.8MB** |

### 1.4 适用场景

- 边缘设备部署（树莓派、$10 开发板）
- 低资源云主机
- 需要快速冷启动的场景
- 多消息平台集成
- 硬件外设控制（STM32、Arduino、GPIO）

---

## 2. 快速开始

### 2.1 一键部署

```bash
# 克隆仓库
git clone https://github.com/zeroclaw-labs/zeroclaw.git
cd zeroclaw

# 一键引导（自动安装依赖）
./bootstrap.sh
```

### 2.2 手动安装

```bash
# 使用 Cargo 构建
cargo build --release --locked

# 安装到系统
cargo install --path . --force --locked

# 或使用 Homebrew（macOS/Linuxbrew）
brew install zeroclaw
```

### 2.3 初始化配置

```bash
# 快速初始化（非交互模式）
zeroclaw onboard --api-key sk-your-api-key --provider openrouter

# 交互式向导（推荐新手）
zeroclaw onboard --interactive

# 仅配置 channels
zeroclaw onboard --channels-only
```

### 2.4 基本使用

```bash
# 单次对话
zeroclaw agent -m "Hello, ZeroClaw!"

# 交互式会话
zeroclaw agent

# 启动网关（默认 127.0.0.1:42617）
zeroclaw gateway

# 启动守护进程（长期运行）
zeroclaw daemon

# 查看系统状态
zeroclaw status

# 运行诊断
zeroclaw doctor
```

### 2.5 验证安装

```bash
# 检查二进制大小
ls -lh $(which zeroclaw)

# 测量启动时间和内存
/usr/bin/time -l zeroclaw --help
/usr/bin/time -l zeroclaw status
```

---

## 3. 核心架构

### 3.1 架构图

ZeroClaw 采用**Trait 驱动架构**，每个子系统都是一个 Trait，通过配置切换实现：

```
┌─────────────────────────────────────────────────────────────┐
│                      ZeroClaw Runtime                        │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ Provider │  │ Channel  │  │   Tool   │  │  Memory  │    │
│  │  (模型)  │  │  (通道)  │  │  (工具)  │  │  (记忆)  │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│       │             │             │             │           │
│  ┌────┴─────────────┴─────────────┴─────────────┴─────┐    │
│  │              Agent Loop (代理循环)                  │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ Security │  │ Gateway  │  │ Observer │  │  Cron    │    │
│  │  (安全)  │  │  (网关)  │  │ (可观测) │  │ (调度器) │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 子系统总览

| 子系统 | Trait | 内置实现 | 扩展方式 |
|--------|-------|----------|----------|
| **AI 模型** | `Provider` | 28+ 内置 Provider | `custom:https://your-api.com` |
| **通道** | `Channel` | CLI, Telegram, Discord, Slack 等 15+ | 任意消息 API |
| **记忆** | `Memory` | SQLite, PostgreSQL, Markdown, None | 任意持久化后端 |
| **工具** | `Tool` | shell, file, browser, http 等 50+ | 实现 Tool trait |
| **可观测性** | `Observer` | Noop, Log, Multi | Prometheus, OTel |
| **运行时** | `RuntimeAdapter` | Native, Docker | 通过 adapter 添加 |
| **安全** | `SecurityPolicy` | 配对、沙箱、allowlist | — |
| **调度器** | `Engine` | Cron 定期任务 | — |
| **技能** | `Loader` | TOML 清单 + SKILL.md | 社区技能包 |

### 3.3 代码结构

```
zeroclaw/
├── src/
│   ├── main.rs           # CLI 入口
│   ├── lib.rs            # 模块导出
│   ├── agent/            # 代理核心逻辑
│   ├── providers/        # AI 模型 Provider
│   ├── channels/         # 消息通道
│   ├── tools/            # 工具实现
│   ├── memory/           # 记忆系统
│   ├── config/           # 配置系统
│   ├── security/         # 安全机制
│   ├── gateway/          # HTTP/WebSocket 网关
│   ├── cron/             # 任务调度
│   ├── peripherals/      # 硬件外设
│   └── ...
├── docs/                 # 文档
├── crates/               # 子 crate
└── Cargo.toml            # 项目配置
```

---

## 4. 配置系统

### 4.1 配置文件位置

配置文件按以下优先级解析：

1. `ZEROCLAW_WORKSPACE` 环境变量
2. `~/.zeroclaw/active_workspace.toml` 标记
3. 默认 `~/.zeroclaw/config.toml`

### 4.2 核心配置项

```toml
# 基础配置
api_key = "sk-your-api-key"
default_provider = "openrouter"
default_model = "anthropic/claude-sonnet-4-6"
default_temperature = 0.7

# 代理配置
[agent]
compact_context = false        # 紧凑上下文（小模型适用）
max_tool_iterations = 10       # 最大工具迭代次数
max_history_messages = 50      # 最大历史消息数
parallel_tools = false         # 并行工具执行

# 记忆系统
[memory]
backend = "sqlite"             # sqlite | lucid | postgres | markdown | none
auto_save = true
embedding_provider = "none"    # none | openai | custom:https://...
vector_weight = 0.7
keyword_weight = 0.3

# 网关配置
[gateway]
host = "127.0.0.1"
port = 42617
require_pairing = true         # 要求配对
allow_public_bind = false      # 禁止公网绑定

# 安全配置
[security.otp]
enabled = false                # 启用 OTP 验证
method = "totp"
gated_actions = ["shell", "file_write", "browser"]

[security.estop]
enabled = false                # 紧急停止
require_otp_to_resume = true

# 可观测性
[observability]
backend = "none"               # none | log | prometheus | otel
runtime_trace_mode = "none"    # none | rolling | full

# 技能系统
[skills]
open_skills_enabled = false    # 启用开放技能
prompt_injection_mode = "full" # full | compact
```

### 4.3 环境变量覆盖

```bash
# Provider 覆盖
export ZEROCLAW_PROVIDER=anthropic

# 工作目录覆盖
export ZEROCLAW_WORKSPACE=/path/to/workspace

# 自动启动 channels
export ZEROCLAW_AUTOSTART_CHANNELS=1
```

### 4.4 导出配置 Schema

```bash
# 导出 JSON Schema
zeroclaw config schema > config-schema.json
```

---

## 5. Provider 系统

### 5.1 什么是 Provider？

Provider 是 AI 模型提供者的抽象，ZeroClaw 通过 `Provider` Trait 统一不同 LLM API。

### 5.2 内置 Provider

| Provider | 说明 |
|----------|------|
| `openrouter` | OpenRouter 聚合平台 |
| `anthropic` | Anthropic (Claude) |
| `openai` | OpenAI (GPT) |
| `openai-codex` | OpenAI Codex (订阅认证) |
| `gemini` | Google Gemini |
| `groq` | Groq 快速推理 |
| `mistral` | Mistral AI |
| `deepseek` | 深度求索 |
| `ollama` | 本地 Ollama |
| `glm` | 智谱 GLM |
| `zai` | 智谱 AI |
| `qwen` | 通义千问 |
| ... | 共 28+ 内置 Provider |

### 5.3 Provider Trait

```rust
#[async_trait]
pub trait Provider: Send + Sync {
    // 查询能力
    fn capabilities(&self) -> ProviderCapabilities;
    
    // 简单聊天
    async fn chat_with_system(
        &self,
        system_prompt: Option<&str>,
        message: &str,
        model: &str,
        temperature: f64,
    ) -> anyhow::Result<String>;
    
    // 带工具调用
    async fn chat_with_tools(
        &self,
        messages: &[ChatMessage],
        tools: &[serde_json::Value],
        model: &str,
        temperature: f64,
    ) -> anyhow::Result<ChatResponse>;
    
    // 流式响应
    fn supports_streaming(&self) -> bool;
    fn stream_chat_with_system(...) -> stream::BoxStream<...>;
}
```

### 5.4 自定义 Provider

```toml
# 使用自定义端点（OpenAI 兼容）
default_provider = "custom:https://your-api.com/v1"

# 使用 Anthropic 兼容端点
default_provider = "anthropic-custom:https://your-anthropic-api.com"
```

### 5.5 切换 Provider

```bash
# 运行时指定
zeroclaw agent --provider anthropic --model claude-sonnet-4-20250514

# 使用订阅认证
zeroclaw agent --provider openai-codex -m "hello"

# 查看 Provider 列表
zeroclaw providers
```

### 5.6 模型管理

```bash
# 刷新模型目录
zeroclaw models refresh
zeroclaw models refresh --provider openrouter --force

# 查看缓存模型
zeroclaw models list
zeroclaw models list --provider anthropic

# 设置默认模型
zeroclaw models set anthropic/claude-sonnet-4-6

# 查看状态
zeroclaw models status
```

---

## 6. Channel 系统

### 6.1 什么是 Channel？

Channel 是消息平台的抽象，让 ZeroClaw 可以接收和发送消息到不同平台。

### 6.2 内置 Channel

| Channel | 说明 |
|---------|------|
| `cli` | 命令行交互 |
| `telegram` | Telegram Bot |
| `discord` | Discord Bot |
| `slack` | Slack Bot |
| `whatsapp` | WhatsApp Business API |
| `matrix` | Matrix (支持 E2EE) |
| `imessage` | iMessage |
| `email` | SMTP/IMAP 邮件 |
| `signal` | Signal |
| `lark` | 飞书 |
| `dingtalk` | 钉钉 |
| `qq` | QQ |
| `irc` | IRC |
| `nostr` | Nostr 协议 |
| `mattermost` | Mattermost |

### 6.3 Channel Trait

```rust
#[async_trait]
pub trait Channel: Send + Sync {
    fn name(&self) -> &str;
    
    // 发送消息
    async fn send(&self, recipient: &str, content: &str) -> Result<()>;
    
    // 监听消息
    async fn listen(&self, cancel_token: CancellationToken) -> Result<()>;
    
    // 健康检查
    async fn health_check(&self) -> Result<()>;
    
    // 输入指示
    async fn typing(&self, recipient: &str) -> Result<()>;
}
```

### 6.4 配置 Channel

```toml
# Telegram
[[channels.telegram]]
bot_token = "YOUR_BOT_TOKEN"
name = "telegram-bot"
allowed_users = ["username1", "username2"]
mention_only = false

# Discord
[[channels.discord]]
bot_token = "YOUR_BOT_TOKEN"
guild_id = "123456789"
name = "discord-bot"

# Slack
[[channels.slack]]
bot_token = "xoxb-YOUR-TOKEN"
name = "slack-bot"
```

### 6.5 Channel 管理

```bash
# 列出所有 Channel
zeroclaw channel list

# 健康检查
zeroclaw channel doctor

# 添加 Channel
zeroclaw channel add telegram '{"bot_token":"...","name":"my-bot"}'

# 移除 Channel
zeroclaw channel remove my-bot

# 绑定 Telegram 用户
zeroclaw channel bind-telegram username
```

### 6.6 运行时命令（Telegram/Discord）

在聊天中发送：
- `/models` - 查看可用模型
- `/models <provider>` - 查看指定 Provider 的模型
- `/model` - 查看当前模型
- `/model <model-id>` - 切换模型
- `/new` - 开始新对话

---

## 7. Tool 系统

### 7.1 什么是 Tool？

Tool 是 AI 代理可以调用的能力单元，通过实现 `Tool` Trait 来扩展。

### 7.2 内置 Tool 分类

| 分类 | Tools |
|------|-------|
| **文件操作** | `file_read`, `file_write`, `file_edit` |
| **Shell 执行** | `shell`, `process` |
| **网络** | `http_request`, `web_search`, `web_fetch` |
| **浏览器** | `browser`, `browser_open`, `screenshot` |
| **记忆** | `memory_store`, `memory_recall`, `memory_forget` |
| **Git** | `git_operations` |
| **调度** | `cron_add`, `cron_list`, `cron_run` |
| **硬件** | `hardware_board_info`, `hardware_memory_read` |
| **通知** | `pushover` |
| **委托** | `delegate` (子代理) |
| **SOP** | `sop_list`, `sop_execute`, `sop_approve` |

### 7.3 Tool Trait

```rust
#[async_trait]
pub trait Tool: Send + Sync {
    // 工具名称
    fn name(&self) -> &str;
    
    // 描述
    fn description(&self) -> &str;
    
    // JSON Schema 参数
    fn parameters_schema(&self) -> serde_json::Value;
    
    // 执行
    async fn execute(&self, args: serde_json::Value) -> anyhow::Result<ToolResult>;
}
```

### 7.4 Tool 执行流程

```
用户请求
    ↓
Agent 解析 → 识别需要调用的 Tool
    ↓
构造 Tool 调用 → 发送 Provider
    ↓
Provider 返回 Tool Call
    ↓
执行 Tool → 获取结果
    ↓
结果反馈给 Provider
    ↓
生成最终回复
```

### 7.5 工具调用示例

```rust
// Tool 调用格式
{
  "name": "shell",
  "arguments": {
    "command": "ls -la"
  }
}
```

### 7.6 并行工具执行

```toml
[agent]
parallel_tools = true  # 启用并行工具执行
```

---

## 8. Memory 系统

### 8.1 记忆系统架构

ZeroClaw 的记忆系统是全栈自研的搜索引擎，零外部依赖：

| 层级 | 实现 |
|------|------|
| **向量数据库** | SQLite BLOB 存储，余弦相似度 |
| **关键词搜索** | FTS5 虚拟表，BM25 评分 |
| **混合合并** | 自定义加权合并 |
| **Embeddings** | `EmbeddingProvider` trait |
| **分块** | 基于行的 Markdown 分块器 |
| **缓存** | SQLite LRU 淘汰 |

### 8.2 Memory Backend

```toml
[memory]
backend = "sqlite"             # 主要后端
auto_save = true               # 自动保存
embedding_provider = "none"    # 嵌入 Provider
```

支持的 Backend：
- `sqlite` - SQLite 混合搜索（默认）
- `lucid` - Lucid 桥接
- `postgres` - PostgreSQL（需启用 feature）
- `markdown` - Markdown 文件存储
- `none` - 无记忆

### 8.3 Memory Trait

```rust
#[async_trait]
pub trait Memory: Send + Sync {
    async fn save(&self, key: &str, value: &str, category: MemoryCategory) -> Result<()>;
    async fn get(&self, key: &str) -> Result<Option<String>>;
    async fn search(&self, query: &str, limit: usize) -> Result<Vec<MemoryEntry>>;
    async fn clear(&self, category: Option<MemoryCategory>) -> Result<()>;
}
```

### 8.4 记忆管理命令

```bash
# 查看记忆统计
zeroclaw memory stats

# 列出记忆
zeroclaw memory list
zeroclaw memory list --category core --limit 10

# 获取特定记忆
zeroclaw memory get <key>

# 清除记忆
zeroclaw memory clear --category conversation --yes
zeroclaw memory clear --key assistant_resp --yes
```

### 8.5 记忆类别

| 类别 | 说明 |
|------|------|
| `core` | 核心记忆（长期） |
| `daily` | 日常记忆 |
| `conversation` | 对话记忆 |
| `custom` | 自定义类别 |

---

## 9. Agent 运行时

### 9.1 Agent 循环

```
┌─────────────────────────────────────────────────────────┐
│                   Agent Loop                            │
├─────────────────────────────────────────────────────────┤
│  1. 接收用户消息                                         │
│  2. 加载相关记忆（RAG）                                   │
│  3. 构建 Prompt（系统提示 + 历史 + 工具）                  │
│  4. 调用 Provider                                        │
│  5. 解析响应（文本 / Tool Calls）                         │
│  6. 执行 Tool Calls（并行/串行）                          │
│  7. 结果反馈给 Provider                                   │
│  8. 重复直到最终回复                                      │
│  9. 保存记忆（可选）                                      │
└─────────────────────────────────────────────────────────┘
```

### 9.2 启动模式

```bash
# CLI 交互模式
zeroclaw agent

# 单次消息模式
zeroclaw agent -m "Summarize today's logs"

# 指定 Provider/Model
zeroclaw agent -p anthropic --model claude-sonnet-4-20250514

# 连接外设
zeroclaw agent --peripheral nucleo-f401re:/dev/ttyACM0
```

### 9.3 守护进程模式

```bash
# 启动守护进程（网关 + channels + 心跳 + 调度器）
zeroclaw daemon

# 指定端口
zeroclaw daemon -p 9090

# 绑定特定 host
zeroclaw daemon --host 127.0.0.1
```

### 9.4 服务管理

```bash
# 安装系统服务（systemd/launchd）
zeroclaw service install

# 启动/停止/重启
zeroclaw service start
zeroclaw service stop
zeroclaw service restart

# 查看状态
zeroclaw service status

# 卸载服务
zeroclaw service uninstall
```

### 9.5 子代理（Delegate）

```toml
# 配置子代理
[agents.researcher]
provider = "openrouter"
model = "anthropic/claude-sonnet-4-6"
system_prompt = "You are a research assistant."
max_depth = 2
agentic = true
allowed_tools = ["web_search", "http_request", "file_read"]
max_iterations = 8

[agents.coder]
provider = "ollama"
model = "qwen2.5-coder:32b"
temperature = 0.2
```

---

## 10. 安全机制

### 10.1 安全默认行为

| 设置 | 默认值 | 说明 |
|------|--------|------|
| `gateway.host` | `127.0.0.1` | 本地绑定 |
| `gateway.port` | `42617` | 默认端口 |
| `require_pairing` | `true` | 要求配对 |
| `allow_public_bind` | `false` | 禁止公网 |
| Channel allowlist | `[]` | 默认拒绝 |

### 10.2 OTP 验证

```toml
[security.otp]
enabled = true
method = "totp"
token_ttl_secs = 30
cache_valid_secs = 300
gated_actions = ["shell", "file_write", "browser"]
gated_domains = ["*.chase.com", "accounts.google.com"]
gated_domain_categories = ["banking"]
```

### 10.3 紧急停止（Estop）

```toml
[security.estop]
enabled = true
state_file = "~/.zeroclaw/estop-state.json"
require_otp_to_resume = true
```

```bash
# 触发紧急停止
zeroclaw estop
zeroclaw estop --level network-kill
zeroclaw estop --level domain-block --domain "*.chase.com"
zeroclaw estop --level tool-freeze --tool shell

# 查看状态
zeroclaw estop status

# 恢复
zeroclaw estop resume
zeroclaw estop resume --network
zeroclaw estop resume --domain "*.chase.com"
zeroclaw estop resume --tool shell
```

### 10.4 沙箱支持

```toml
# Docker 沙箱
[runtime]
kind = "docker"

# Landlock（Linux）
# 启用 feature: sandbox-landlock
```

### 10.5 资源限制

```toml
[security.resource_limits]
max_file_size = 10_485_760      # 10MB
max_shell_output = 1_048_576    # 1MB
rate_limit_per_minute = 60
```

---

## 11. 硬件外设

### 11.1 支持的硬件

| 硬件 | 说明 |
|------|------|
| `nucleo-f401re` | STM32 Nucleo 开发板 |
| `arduino-uno` | Arduino Uno |
| `esp32` | ESP32 开发板 |
| `rpi-gpio` | 树莓派 GPIO |

### 11.2 硬件发现

```bash
# 发现 USB 设备
zeroclaw hardware discover

# 设备内省
zeroclaw hardware introspect /dev/ttyACM0

# 获取芯片信息
zeroclaw hardware info --chip STM32F401RETx
```

### 11.3 配置外设

```toml
[[peripherals]]
board = "nucleo-f401re"
path = "/dev/ttyACM0"

[[peripherals]]
board = "rpi-gpio"
path = "native"
```

```bash
# 添加外设
zeroclaw peripheral add nucleo-f401re /dev/ttyACM0
zeroclaw peripheral add rpi-gpio native

# 列出外设
zeroclaw peripheral list
```

### 11.4 固件烧录

```bash
# 烧录 Arduino
zeroclaw peripheral flash --port /dev/cu.usbmodem12345

# 烧录 Nucleo
zeroclaw peripheral flash-nucleo

# 设置 Arduino Uno Q Bridge
zeroclaw peripheral setup-uno-q --host 192.168.0.48
```

---

## 12. 技能系统

### 12.1 什么是技能？

技能是用户定义的能力扩展，通过 `SKILL.toml` 清单和 `SKILL.md` 指令文件定义。

### 12.2 技能结构

```
my-skill/
├── SKILL.toml          # 技能清单
├── SKILL.md            # 技能指令
├── tools/              # 工具定义
│   └── my_tool.toml
└── prompts/            # Prompt 模板
    └── system.md
```

### 12.3 SKILL.toml 示例

```toml
name = "my-skill"
version = "1.0.0"
description = "我的自定义技能"

[[tools]]
name = "my_custom_tool"
description = "自定义工具"

[prompts]
system = "prompts/system.md"
```

### 12.4 技能管理

```bash
# 列出技能
zeroclaw skills list

# 审计技能（安全检查）
zeroclaw skills audit my-skill
zeroclaw skills audit /path/to/skill

# 安装技能
zeroclaw skills install https://github.com/user/my-skill
zeroclaw skills install ./local-skill

# 移除技能
zeroclaw skills remove my-skill
```

### 12.5 安全审计

技能安装前会自动进行安全审计，阻止：
- 符号链接
- 脚本文件（`.sh`, `.bash`, `.ps1` 等）
- 高风险命令（管道执行等）
- 外部链接

---

## 13. 实战练习

### 练习 1：配置第一个 Agent

```bash
# 1. 安装 ZeroClaw
cargo install --path . --force

# 2. 初始化配置
zeroclaw onboard --interactive

# 3. 测试对话
zeroclaw agent -m "你好，请介绍一下自己"

# 4. 查看配置
zeroclaw status
```

### 练习 2：配置 Telegram Bot

```bash
# 1. 在 Telegram 创建 Bot（@BotFather）
# 2. 获取 Bot Token

# 3. 添加 Channel
zeroclaw channel add telegram '{"bot_token":"YOUR_TOKEN","name":"my-telegram"}'

# 4. 绑定用户
zeroclaw channel bind-telegram your_username

# 5. 启动守护进程
zeroclaw daemon

# 6. 在 Telegram 与 Bot 对话
```

### 练习 3：创建定时任务

```bash
# 1. 启用 Cron（config.toml）
[cron]
enabled = true

# 2. 添加定时任务
zeroclaw cron add '0 9 * * 1-5' 'Good morning! Today is {{weekday}}' --tz America/New_York

# 3. 添加一次性任务
zeroclaw cron add-at 2025-01-15T14:00:00Z 'Send reminder'

# 4. 添加间隔任务
zeroclaw cron add-every 60000 'Ping heartbeat'

# 5. 查看任务
zeroclaw cron list
```

### 练习 4：使用记忆系统

```bash
# 1. 与 Agent 对话（自动保存记忆）
zeroclaw agent

# 2. 查看记忆
zeroclaw memory stats
zeroclaw memory list

# 3. 搜索记忆
zeroclaw agent -m "还记得我昨天说的项目吗？"
```

### 练习 5：配置子代理

```toml
# config.toml
[agents.researcher]
provider = "openrouter"
model = "anthropic/claude-sonnet-4-6"
agentic = true
allowed_tools = ["web_search", "http_request"]

[agents.coder]
provider = "ollama"
model = "qwen2.5-coder:32b"
```

```bash
# 测试委托
zeroclaw agent -m "请研究一下 Rust 的最新发展，然后写个示例代码"
```

---

## 14. 进阶主题

### 14.1 自定义 Provider

实现 `Provider` Trait：

```rust
use zeroclaw::providers::{Provider, ProviderCapabilities, ChatMessage, ChatResponse};

struct MyProvider {
    api_key: String,
    api_url: String,
}

#[async_trait]
impl Provider for MyProvider {
    fn capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            native_tool_calling: true,
            vision: false,
        }
    }

    async fn chat_with_system(
        &self,
        system_prompt: Option<&str>,
        message: &str,
        model: &str,
        temperature: f64,
    ) -> anyhow::Result<String> {
        // 实现你的 API 调用逻辑
        Ok("response".to_string())
    }
}
```

### 14.2 自定义 Tool

```rust
use zeroclaw::tools::{Tool, ToolResult};

struct MyTool;

#[async_trait]
impl Tool for MyTool {
    fn name(&self) -> &str {
        "my_tool"
    }

    fn description(&self) -> &str {
        "我的自定义工具"
    }

    fn parameters_schema(&self) -> serde_json::Value {
        serde_json::json!({
            "type": "object",
            "properties": {
                "param1": {"type": "string"}
            },
            "required": ["param1"]
        })
    }

    async fn execute(&self, args: serde_json::Value) -> anyhow::Result<ToolResult> {
        let param1 = args["param1"].as_str().unwrap_or("");
        // 实现工具逻辑
        Ok(ToolResult {
            success: true,
            output: format!("Executed with param: {}", param1),
            error: None,
        })
    }
}
```

### 14.3 自定义 Channel

```rust
use zeroclaw::channels::{Channel, SendMessage};

struct MyChannel {
    name: String,
    // 你的连接/客户端
}

#[async_trait]
impl Channel for MyChannel {
    fn name(&self) -> &str {
        &self.name
    }

    async fn send(&self, recipient: &str, content: &str) -> Result<()> {
        // 实现发送逻辑
        Ok(())
    }

    async fn listen(&self, cancel_token: CancellationToken) -> Result<()> {
        // 实现监听循环
        Ok(())
    }

    async fn health_check(&self) -> Result<()> {
        // 实现健康检查
        Ok(())
    }
}
```

### 14.4 可观测性集成

```toml
# Prometheus
[observability]
backend = "prometheus"

# OpenTelemetry
[observability]
backend = "otel"
otel_endpoint = "http://localhost:4318"
otel_service_name = "zeroclaw"

# 运行时追踪
runtime_trace_mode = "rolling"
runtime_trace_path = "state/runtime-trace.jsonl"
runtime_trace_max_entries = 200
```

```bash
# 查询追踪
zeroclaw doctor traces --limit 20
zeroclaw doctor traces --event tool_call_result --contains "error"
```

### 14.5 部署到边缘设备

```bash
# 交叉编译（以 ARM 为例）
rustup target add armv7-unknown-linux-gnueabihf
cargo build --release --target armv7-unknown-linux-gnueabihf

# 或使用 Docker
docker build -t zeroclaw .
docker run -d zeroclaw daemon
```

---

## 附录 A：常用命令速查

```bash
# 安装/更新
cargo install --path . --force
brew install zeroclaw

# 初始化
zeroclaw onboard --interactive
zeroclaw onboard --api-key sk-xxx --provider openrouter

# 运行
zeroclaw agent -m "message"
zeroclaw daemon
zeroclaw gateway -p 8080

# 状态/诊断
zeroclaw status
zeroclaw doctor
zeroclaw doctor models
zeroclaw doctor traces

# Channel
zeroclaw channel list
zeroclaw channel doctor
zeroclaw channel add telegram '...'

# Cron
zeroclaw cron list
zeroclaw cron add '0 9 * * *' 'message'

# Memory
zeroclaw memory stats
zeroclaw memory list

# Models
zeroclaw models list
zeroclaw models refresh

# Service
zeroclaw service install
zeroclaw service status
```

---

## 附录 B：故障排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 启动失败 | 配置错误 | `zeroclaw status` 检查配置 |
| Channel 不工作 | Token 过期 | 重新配置 Channel |
| 内存占用高 | 历史消息过多 | 调整 `max_history_messages` |
| 工具调用失败 | Provider 不支持 | 检查 Provider 能力 |
| 网关无法访问 | 绑定地址限制 | 检查 `allow_public_bind` |

---

## 附录 C：学习资源

- **官方文档**: https://github.com/zeroclaw-labs/zeroclaw/tree/main/docs
- **命令参考**: `docs/commands-reference.md`
- **配置参考**: `docs/config-reference.md`
- **运维手册**: `docs/operations-runbook.md`
- **安全文档**: `docs/security/README.md`
- **硬件文档**: `docs/hardware/README.md`

---

*教程版本：2026 年 2 月 26 日*
*ZeroClaw 版本：0.1.7*
